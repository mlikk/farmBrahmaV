
// This layout provides a clean, navigation-free structure for the partner landing page.
export default function PartnerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
